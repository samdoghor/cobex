
import useScrollToHash from './useScrollToHash';

const ScrollToHashWrapper = () => {
    useScrollToHash(); // Call the hook here, inside a functional component
    return null;
};

export default ScrollToHashWrapper;
